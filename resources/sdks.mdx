---
title: SDKs
description: Official Widgetic SDKs and libraries
---

Widgetic provides SDKs to simplify integration with our platform.

## Official SDKs

<CardGroup cols={2}>
  <Card title="JavaScript / TypeScript API SDK" icon="js">
    Primary SDK for Node.js and modern browsers
  </Card>
  <Card title="Embed SDK" icon="code">
    Lightweight SDK for embedding widgets
  </Card>
  <Card title="UI SDK" icon="window">
    For partners embedding UI components
  </Card>
</CardGroup>

<Tip>
  We currently publish official SDK packages for **JavaScript/TypeScript**, **Embed**, and **UI**.
  For other languages, use the multi-language code samples in the API Reference (or cURL).
</Tip>

## JavaScript / TypeScript API SDK

The primary SDK for interacting with the Widgetic API.

### Installation

<CodeGroup>

```bash npm
npm install @widgetic/api-sdk
```

```bash yarn
yarn add @widgetic/api-sdk
```

```bash pnpm
pnpm add @widgetic/api-sdk
```

</CodeGroup>

### Basic Usage

```javascript
import { WidgetsApi } from '@widgetic/api-sdk';

// Initialize with API key
const widgets = new WidgetsApi('YOUR_API_KEY');

// Fetch widgets
const result = await widgets.getAllWidgets({ page: 1, size: 20 });
console.log(result);
```

<Tip>
  You can use either API keys (recommended for server-side) or JWTs depending on your integration.
  See [Authentication](/docs/authentication) for details.
</Tip>

### Available APIs

| Class | Description |
|-------|-------------|
| `WidgetsApi` | Widget management |
| `CompositionsApi` | Composition management |
| `CanvasesApi` | Canvas operations |
| `ComponentsApi` | Component management |
| `TokensApi` | Token exchange and validation |
| `EntitlementsApi` | Feature access |
| `UsersApi` | User management |
| `ManagedUsersApi` | Partner user management |
| `UIApi` | UI component embedding |
| `PartnersApi` | Partner operations |

## Embed SDK

Lightweight SDK for embedding widgets on your website.

### Installation

<CodeGroup>

```bash npm
npm install @widgetic/embed-sdk
```

```bash yarn
yarn add @widgetic/embed-sdk
```

```bash pnpm
pnpm add @widgetic/embed-sdk
```

</CodeGroup>

### Usage

```javascript
import { embed, embedComposition } from '@widgetic/embed-sdk';

// Embed a single widget
embed({
  widgetId: 'widget-uuid',
  container: '#widget-container',
  responsive: true
});

// Embed a composition
embedComposition({
  compositionId: 'composition-uuid',
  container: '#composition-container'
});
```

### Features

- **Progressive enhancement**: Falls back gracefully
- **Responsive**: Adapts to container size
- **HTTP/3 support**: Optimized loading
- **Lazy loading**: Load widgets on demand

## UI SDK (Partners)

For partners embedding Widgetic UI components.

### Installation

<CodeGroup>

```bash npm
npm install @widgetic/ui-sdk
```

```bash yarn
yarn add @widgetic/ui-sdk
```

```bash pnpm
pnpm add @widgetic/ui-sdk
```

</CodeGroup>

### Usage

```javascript
import { WidgeticUI } from '@widgetic/ui-sdk';

const ui = new WidgeticUI({
  apiKey: 'wgt_prt_your_partner_key'
});

// Mount widget creator
await ui.mount('widget-creator', {
  container: '#editor-container',
  theme: 'dark',
  onSave: (widget) => console.log('Widget saved:', widget)
});
```

## Browser Usage

### ESM Import

```html
<script type="module">
  import { WidgetsApi } from 'https://cdn.widgetic.com/sdk/api-sdk.esm.js';
  
  const widgets = new WidgetsApi('YOUR_API_KEY');
  const result = await widgets.getAllWidgets();
  console.log(result);
</script>
```

### Script Tag

```html
<script src="https://cdn.widgetic.com/sdk/embed-sdk.umd.js"></script>
<script>
  Widgetic.embed({
    widgetId: 'widget-uuid',
    container: '#widget-container'
  });
</script>
```

## Multi-Language Code Samples (API Reference)

Our [API Reference](/api-reference/introduction) includes code samples in **8 languages** for every public endpoint:

| Language | Sample Type |
|----------|-------------|
| **cURL** | HTTP examples |
| **Node.js** | `@widgetic/api-sdk` |
| **Python** | HTTP examples |
| **Go** | HTTP examples |
| **Ruby** | HTTP examples |
| **PHP** | HTTP examples |
| **Java** | HTTP examples |
| **C#** | HTTP examples |

These samples are maintained to stay in sync with the public API surface.

<Info>
  Want an official SDK published for your language (PyPI, Maven, NuGet, RubyGems, Packagist, etc.)?
  Tell us your target registry and preferred package name/versioning.
</Info>

## Need Help?

- [API Reference](/api-reference/introduction)
- [Code Examples](/resources/examples)
- [GitHub Issues](https://github.com/widgetic/issues)
